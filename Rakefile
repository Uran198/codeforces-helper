# This file is for
#  generating boilerplate main.cpp
#  fetching tests
#  instead of Makefile
#  running on sample tests

task :default => [:test, :test1]

task :test do
  sh 'echo "Test"'
  mkdir "jijiji"
end

task :test1 do
  sh 'echo "Test1"'
end
